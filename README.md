# Europe Travel Selector

An interactive Europe trip selector built with React and Vite.

## Local development

```bash
npm install
npm run dev
```

## Publishing

Push the `main` branch to a public GitHub repository named
`europe-travel-selector`. The included GitHub Actions workflow builds the site
and publishes it to GitHub Pages automatically.
